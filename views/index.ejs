<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather App</title>
    <link rel="stylesheet" href="main.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <div class="container">
        <!-- Form for user input -->
        <form id="myForm" method="post">
            <input type="text" id="cityInput" placeholder="Enter city.." name="name" />
            <button type="submit" id="search" formaction="/search-name">
                <i class="fa-solid fa-magnifying-glass"></i>
            </button>
        
        <div class="response">
            <div class="weather-temp">
                <!-- Conditional rendering of weather information -->
                <% if(content.icon1 && content.temp && content.city && content.humid && content.wind1) {%>
                <img class="icon" src="http://openweathermap.org/img/wn/<%= content.icon1 %>@2x.png" alt="Weather Icon">
                <p class="temp"><%= content.temp %>°c</p>
                <p class="city"><%= content.city %></p>
            </div>

            <div class="weather-humidity-wind">
                <p class="humid">Humidity: <%= content.humid %>%</p>
                <p class="wind">Wind Speed: <%= content.wind1 %> km/h</p>
            </div>
            <% } else { %> <!-- Fixed: added space for consistent formatting -->
                <!-- Display error or message when no weather data is available -->
                <%= content %>
            <% } %>
        </div>
    </div>
</body>
</html>
